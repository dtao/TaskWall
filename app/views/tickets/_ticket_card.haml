.ticket-card(class="#{classes_for_ticket_card(ticket)}")
  .id
    = ticket.unfuddle_id
  .number
    = ticket.unfuddle_number
    .icons
      - if ticket.updates.any?
        .icon.has-updates
      - if ticket.comments.any?
        .icon.has-comments
  .summary= ticket.summary
